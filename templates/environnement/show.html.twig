{% extends 'admin-base.html.twig' %}

{% block title %}Environnement{% endblock %}

{% block body %}
    <h1>Environnement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ environnement.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ environnement.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('environnement_index') }}">back to list</a>

    <a href="{{ path('environnement_edit', {'id': environnement.id}) }}">edit</a>

    {{ include('environnement/_delete_form.html.twig') }}
{% endblock %}
