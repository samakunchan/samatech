{{ form_start(form) }}
    <div class="form-group">
        {{ form_errors(form.type) }}
        {{ form_label(form.type, 'Titre') }}
        {{ form_widget(form.type, { 'attr': {'class': 'form-control'} }) }}
    </div>
    <button class="btn">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}

